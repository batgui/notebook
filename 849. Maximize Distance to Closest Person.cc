class Solution {
public:
	vector<int > visit;
    int maxDistToClosest(vector<int>& seats) {
        
        for (int i =0; i < seats.size(); i ++) {
        	if (seats[i] == 1) {
        		process[seat];
        	}

        }
    }
    void process(int seat ) {
    	
    }
};