最大连续子数组

 分治法：

把数组从中间分开，最大的子数组要么全在左边，要么在右边，要么跨立在分界点上。

完全在左，右数组的可以用递归解决
。
跨立在分界点上，是左数组额最大后缀和有数组的最大前缀之和，从分界点向前扫，向后扫即可。

分析法
计算数组的前缀和
则s[i,j] = p[j] - p[i-1]

进一步分析我们可以记S[i] 为以A[i]结尾的数组中和最大的子数组，则S[i + 1] = max(S[i] + A[i + 1], A[i + 1]) 这就可以转化为动态规划问题。


零子数组
求子数组的和接近0的子数组。
设一个sum数组其中sum[i]是A的前i项和，然后排序，计算相邻元素的差。


LCS最长公共子序列

LCS(X,Y) 为字符串x和y的最长公共子序列
如果xm = yn 则 最长公共子序列为LCS(Xm, Yn) = LCS(Xm-1, Yn-1) + Xm
 若不相等则

 LCS(Xm, Yn) 为max( LCS(Xm-1, Yn), LCS(Xm, Yn-1))
